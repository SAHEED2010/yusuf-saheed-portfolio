import { verifySubscription } from "@/newsletter/store";

export default async function VerifySubscriptionPage({ searchParams }: { searchParams: Promise<{ token?: string }> }) { const { token } = await searchParams; const verified = token ? await verifySubscription(token) : false; return <section className="detail"><p className="eyebrow">Subscription verification</p><h1>{verified ? "Subscription verified." : "Verification link unavailable."}</h1><p className="lede">{verified ? "You will receive future release notifications for published work." : "This verification link is missing, invalid, or expired. Submit the form again to request a new one."}</p></section>; }
